version: 0.0.3
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
name: deepseek-ocr
repository: https://github.com/oomol-flows/deepseek-ocr
releaseNotes: init
displayName: "%display-name%"
icon: ./icon.png
description: "%deepseek-ocr-ocr-markdown%"
